<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, tasks_path(id: list.id) %> - <%= link_to 'Change name', edit_list_path(list)  %></li>
  <% end %>
</ul>

<%= link_to 'New list', new_list_path, class:'btn btn-primary'  %>
